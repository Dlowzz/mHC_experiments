
#

1.把 beta 提前到 rank 空间

————————

一个参考的实现例子：

down = einsum(

    branch_output,

    self.stream_down_weight,

    "b ... f d, s d r -> b ... f s r",

)

down = rmsnorm_lastdim(down)

# 先在 rank 维进行 beta write

down_write = einsum(

    down,

    beta,

    "b ... f1 s r, b ... f1 s f2 -> b ... f2 s r",

)

# 再升维到 hidden dimension

lora_write = einsum(

    down_write,

    self.stream_up_weight,

    "b ... f s r, s r d -> b ... f s d",

)

output = base_write + self.lora_lambda * lora_write

————————

并且我现在是num_fracs=1，完全可以省掉第二个beta einsum

# branch_output: [B, T, 1, C]

# beta:         [B, T, 1, S, 1]

# down:         [B, T, 1, S, R]

beta_s = beta[..., 0, :, 0]                 # [B, T, S]

h = branch_output[..., 0, :]                # [B, T, C]

down = down[..., 0, :, :]                   # [B, T, S, R]

down_write = down * beta_s.unsqueeze(-1)    # beta only touches rank R=8

lora_write = einsum(

    down_write,

    self.stream_up_weight,

    "b ... s r, s r d -> b ... s d",

)

output = h.unsqueeze(-2) * beta_s.unsqueeze(-1)

output = output + self.lora_lambda * lora_write

2.把 LoRA 的 A 投影改成单个大 GEMM，因为所有 As的输入都是同一个 h，可以把它们横向拼起来

3.B 投影改成显式 batched GEMM

4.把group设计也同步进行优化：定义：

x=[X

1,1

    

,X

1,2

    

,…,X

n,g

    

]∈R

nC

构造稀疏矩阵：

W

group-dense

    

∈R

nC×gn

其中输出 (q,t) 只连接输入中属于 group q 的通道，其余权重固定为 0。

那么：

L

group

    

=xW

group-dense

    

与原来的逐 group 运算严格等价。

优点：

不再做 [s,q,d] → [q,s,d] 的全量 copy；

一个标准 GEMM；

输出只有 gn=16 维；

GPU 会更喜欢一个略有冗余的大 GEMM，而不是多个小 contraction 和 transpose。

缺点是 dense GEMM 会计算固定为零的连接。默认 n=g=4，计算量会比紧凑 group linear 多约 4 倍。

5.将三个 gate projection 合并成一次 GEMM

group 版本目前对同一个 normed 分别计算：

dynamic_alpha_fn；

dynamic_beta_fn；

group_pre_weight。

其中 dynamic_alpha_fn 还会生成原始 mHC 的 Hpre 部分，但 group 模式下原始 Hpre 已经被替换，相关输出最终没有使用。当前 width path 确实只取 alpha[..., num_input_views:] 作为 residual mixing。

可以定义一次 packed projection：

W

gate

    

=[W

res

    

∣W

β

    

∣W

group

    

]

然后：

[ℓ

res

    

,ℓ

β

    

,ℓ

group

    

]=xW

gate

    

代码形式：

gate_raw = F.linear(normed, self.gate_weight_packed)

res_raw, beta_raw, group_raw = torch.split(

    gate_raw,

    [S * S, S, G * S],

    dim=-1,

)

再分别：

residual logits → Sinkhorn；

beta logits → sigmoid × 2；

group logits → sigmoid。

数学上：

x[W

1

    

∣W

2

    

∣W

3

    

]=[xW

1

    

∣xW

2

    

∣xW

3

    

]

所以完全等价。
